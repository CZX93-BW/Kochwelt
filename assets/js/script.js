// Funktion zum Ein- und Ausblenden des Menüs
function toggleMenu() {
  
  // Toggle der Klasse "show" auf dem Menü-Element
  // Wenn "show" gesetzt ist, wird das Menü sichtbar, sonst unsichtbar
  document.getElementById("menu").classList.toggle("show");
}